# API target: local | test | production
VITE_ENV=local

# Optional: override local API URL (default http://127.0.0.1:3900)
# VITE_API_URL=http://127.0.0.1:3900

# Vercel: set VITE_ENV in Project → Settings → Environment Variables, then redeploy.
#   Production (holisticare.vercel.app)     → VITE_ENV=production
#   Preview/Staging (holisticare-develop...)  → VITE_ENV=test
# Do not use VITE_ENV=local on Vercel deployments.
# Production/test API (Vercel proxy → Azure VM):
#   https://vercel-backend-one-roan.vercel.app/service

# Progressive lab processing UI (requires backend ENABLE_ASYNC_PROCESSING=true)
# VITE_ENABLE_ASYNC_PROCESSING=true
# Early overview preview works without async; incremental scoring uses ENABLE_INCREMENTAL_SCORING on backend
